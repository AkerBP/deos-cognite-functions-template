import pandas as pd

def transformation(input_parameters: dict, df: pd.DataFrame):
    """_summary_

    Args:
        input_parameters (dict): _description_
        df (pd.DataFrame): _description_
    """
    pass